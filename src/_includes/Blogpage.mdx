---
layout: Blog.tsx
title: Blog
---

<div class="markdown">
  # Blog
</div>

<div>
  {page.data.results.map((page) => (
    <a href={page.data.url}>
      <figure class="my-8">
        <div class="flex justify-between flex-wrap mb-1 items-end">
          <h2 class="text-3xl font-light text-primary dark:text-primary-dark">{page.data.title}</h2>
            <span>
              {new Date(page.data.date).toLocaleDateString("de-DE", {
                year: "numeric",
                month: "long",
                day: "numeric",
              })}
            </span>
          </div>
        <div class="border-solid border-2 border-gray-600 dark:border-gray-400 no-underline">
          {page.data.imgUrl ? (
            <img class="w-full" src={page.data.imgUrl} />
          ) : null}
          <div class="m-3">{page.data.excerpt}</div>
        </div>
      </figure>
    </a>

))}
</div>

<nav>
  <ul class="pagination">
    {page.data.pagination.previous ? (
      <li class="page-item">
        <a
          class="page-link btn btn-secondary"
          href={`/blog/${page.data.pagination.previous}`}
        >
          &laquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link btn btn-secondary disabled">&laquo;</a>
      </li>
    )}
    {new Array(page.data.pagination.totalPages).fill().map((_, index) => (
      <li class="page-item">
        <a class="page-link btn btn-secondary" href={`/blog/page/${index + 1}`}>
          {index + 1}
        </a>
      </li>
    ))}
    {page.data.pagination.next ? (
      <li class="page-item">
        <a
          class="page-link btn btn-secondary"
          href={`/blog/${page.data.pagination.next}`}
        >
          &raquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link btn btn-secondary disabled">&raquo;</a>
      </li>
    )}
  </ul>
</nav>

<a class="btn btn-primary my-2" href="/feed.rss">RSS-Feed</a>
